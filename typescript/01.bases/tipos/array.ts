(() => {
    // const numbers: (string | number | boolean)[] = [1, 'c', true];
    const number: number[] = [1, 2, 3];
    const villians: string[] = ['Joker', 'Harley Quinn'];

    villians.forEach(v => console.log(v.toUpperCase()));
})();